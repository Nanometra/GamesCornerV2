<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.1" xmlns="http://xmlns.jcp.org/xml/ns/persistence" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd">
	<persistence-unit name="GamesCornerV2_PU" transaction-type="RESOURCE_LOCAL">
		<provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
		<properties>
			<property name="${driver}" value="${projet.driver}"/>
			<property name="${url}" value="${projet.url}"/>
			<property name="${username}" value="${projet.username}"/>
			<property name="${password}" value="${projet.password}"/>
			<property name="${dialect}" value="${projet.dialect}"/>
			<property name="${sql}" value="${projet.show_sql}"/>
			<property name="${schema-generation}" value="${projet.schema-generation.hbm2ddl.auto}"/>
		</properties>
	</persistence-unit>
</persistence>
